//#region src/utils/guideLineAncestors.ts
/**
* Build a child→parent map from treeData. When `flattenEmptyDirectories` is
* enabled, intermediate nodes hidden inside flattened chains are skipped, and
* children of composite flattened nodes map to the earliest segment
* (`flattens[0]`) because that's the visual level where the flattened row sits.
*/
function buildChildToParent(treeData, flattenEmptyDirectories) {
	const flattenedIntermediates = /* @__PURE__ */ new Set();
	const flattenedFirstSegment = /* @__PURE__ */ new Map();
	if (flattenEmptyDirectories) {
		for (const [id, node] of Object.entries(treeData)) if (node.flattens != null && node.flattens.length > 0) {
			flattenedFirstSegment.set(id, node.flattens[0]);
			for (const intermediateId of node.flattens) flattenedIntermediates.add(intermediateId);
		}
	}
	const map = /* @__PURE__ */ new Map();
	for (const [id, node] of Object.entries(treeData)) {
		if (flattenedIntermediates.has(id) || node.flattens != null) continue;
		const childIds = flattenEmptyDirectories ? node.children?.flattened ?? node.children?.direct ?? [] : node.children?.direct ?? [];
		for (const childId of childIds) map.set(childId, id);
	}
	for (const [compositeId, firstSegmentId] of flattenedFirstSegment) {
		const compositeParent = map.get(compositeId);
		if (compositeParent != null) map.set(firstSegmentId, compositeParent);
		const compositeNode = treeData[compositeId];
		const childIds = [...compositeNode?.children?.direct ?? [], ...compositeNode?.children?.flattened ?? []];
		for (const childId of childIds) map.set(childId, firstSegmentId);
	}
	return map;
}
/**
* Build per-item ancestor chains: itemId → [level0Ancestor, level1Ancestor, ...]
* Uses childToParent to walk up the tree from each node.
*/
function buildAncestorChains(treeData, childToParent) {
	const chains = /* @__PURE__ */ new Map();
	const getChain = (id) => {
		const cached = chains.get(id);
		if (cached != null) return cached;
		const parentId = childToParent.get(id);
		if (parentId == null || parentId === "root") {
			const chain$1 = [];
			chains.set(id, chain$1);
			return chain$1;
		}
		const chain = [...getChain(parentId), parentId];
		chains.set(id, chain);
		return chain;
	};
	for (const id of Object.keys(treeData)) getChain(id);
	return chains;
}

//#endregion
export { buildAncestorChains, buildChildToParent };
//# sourceMappingURL=guideLineAncestors.js.map