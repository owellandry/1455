import { fileListToTree } from "../utils/fileListToTree.js";

//#region src/loader/sync.ts
/**
* Creates a sync data loader that pre-builds all nodes upfront.
* Best for small-to-medium trees or workflows that touch most nodes.
* Tradeoff: higher upfront cost, but faster random access afterward.
*
* @param filePaths - Array of file path strings
* @param options - Configuration options
*/
function generateSyncDataLoader(filePaths, options = {}) {
	const { flattenEmptyDirectories = false, rootId, rootName, sortComparator } = options;
	const tree = fileListToTree(filePaths, {
		rootId,
		rootName,
		sortComparator
	});
	return {
		getItem: (id) => tree[id],
		getChildren: (id) => {
			const children = tree[id]?.children;
			if (children == null) return [];
			if (flattenEmptyDirectories === true) {
				if (children.flattened != null) return children.flattened;
			}
			return children.direct;
		}
	};
}

//#endregion
export { generateSyncDataLoader };
//# sourceMappingURL=sync.js.map