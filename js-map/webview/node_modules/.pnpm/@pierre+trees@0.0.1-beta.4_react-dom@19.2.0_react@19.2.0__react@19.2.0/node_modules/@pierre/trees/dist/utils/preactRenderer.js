import { Root } from "../components/Root.js";
import { hydrate, render } from "preact";
import { jsx } from "preact/jsx-runtime";

//#region src/utils/preactRenderer.tsx
/**
* Mutable renderer implementation so tests can stub Preact rendering/hydration
* in jsdom (Preact 11 beta can crash there).
*/
const preactRenderer = {
	renderRoot: (element, props) => {
		render(/* @__PURE__ */ jsx(Root, { ...props }), element);
	},
	hydrateRoot: (element, props) => {
		hydrate(/* @__PURE__ */ jsx(Root, { ...props }), element);
	},
	unmountRoot: (element) => {
		render(null, element);
	}
};
function preactRenderRoot(element, props) {
	preactRenderer.renderRoot(element, props);
}
function preactHydrateRoot(element, props) {
	preactRenderer.hydrateRoot(element, props);
}
function preactUnmountRoot(element) {
	preactRenderer.unmountRoot(element);
}

//#endregion
export { preactHydrateRoot, preactRenderRoot, preactRenderer, preactUnmountRoot };
//# sourceMappingURL=preactRenderer.js.map